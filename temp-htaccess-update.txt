RewriteEngine On
RewriteBase /

# Proxy API requests to Node.js backend (port 3001)
RewriteCond %{REQUEST_URI} ^/api/(.*)$
RewriteRule ^api/(.*)$ http://127.0.0.1:3001/api/$1 [P,L]

# Proxy health check to Node.js backend  
RewriteCond %{REQUEST_URI} ^/health$
RewriteRule ^health$ http://127.0.0.1:3001/health [P,L]

# Client-side routing for React (must come after API rules)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_URI} !^/api/
RewriteCond %{REQUEST_URI} !^/health$
RewriteRule . /index.html [L]


# Updated .htaccess for ~/www-root/.htaccess
# Copy the above content to your ~/www-root/.htaccess file:
# 
# cd ~/www-root
# cp /path/to/temp-htaccess-update.txt .htaccess
# 
# Then restart your API server:
# cd ~/api
# pkill -f "node.*server.js" || true
# node server.js &
#
# Test the setup:
# curl http://vdondeti.w3.uvm.edu/health
# curl http://vdondeti.w3.uvm.edu/api/databases